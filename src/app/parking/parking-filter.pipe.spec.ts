import { ParkingFilterPipe } from './parking-filter.pipe';

describe('ParkingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});